import { View } from 'react-native';
// Placeholder route. The tabPress listener intercepts navigation here
// and opens a bottom sheet instead. This file just needs to exist so
// Expo Router accepts the Tabs.Screen name='fab'.
export default function FabRoute() {
  return <View />;
}
